알겠습니다, 아길이님 👍
아래는 **방금 제시한 AGENTS.md를 아길이님 구조에 맞게, 완전 한글로 다시 작성한 버전**입니다.
(의미·강도·통제력은 그대로 유지하고, 한국어 개발 환경에서 바로 쓰기 좋게 다듬었습니다)

---

# ✅ AGENTS.md

### NEXA Platform v2 – AI 에이전트 핵심 통제 규칙 (ROOT)

> 이 문서는 **AI 에이전트를 ‘설계자’가 아닌 ‘보조자’로 제한하기 위한 최상위 규칙**이다.
> AI는 구조를 해석하거나 재설계하지 않는다.

---

## 1️⃣ 전역 원칙 (항상 적용)

-   빠른 구현보다 **구조적 안정성**을 우선한다.
-   편의성, 축약, 요령을 위한 코드는 허용하지 않는다.
-   맥락이 불분명하면 **추측하지 말고 반드시 질문한다**.
-   한 번에 큰 변경보다 **작고 되돌릴 수 있는 변경**을 선호한다.
-   AI는 “판단자”가 아니라 **지시에 따라 움직이는 작업자**다.

---

## 2️⃣ 절대 수정 금지 영역 (No-Touch Zone)

아래 디렉터리는 **플랫폼의 계약(Contract) 계층**이며
**명시적인 지시 없이는 절대 수정하지 않는다.**

-   `/src/system/**`
-   `/src/frame/**`
-   `/src/engines/**`
-   `/src/assets/**`
-   `/public/**`

### 이유

-   전역 규칙
-   플랫폼 프레임
-   핵심 엔진
-   디자인 시스템
-   공용 리소스

👉 이 영역을 변경하면 **전체 시스템 안정성이 붕괴**된다.

---

## 3️⃣ 조건부 수정 허용 영역

### `/src/domains/**`

-   각 도메인은 **독립된 프로젝트 단위**다.
-   AI는 **한 번에 하나의 도메인만** 수정할 수 있다.
-   도메인 간 교차 수정은 **절대 금지**한다.

### 허용

-   `views/` 내부 UI 구현
-   도메인 전용 `components`
-   도메인 로컬 `store` 로직

### 금지

-   system 계층으로 로직 이동
-   전역 schema 생성 또는 수정
-   frame / layout 계약 변경
-   다른 도메인 코드 참조

---

## 4️⃣ 레이아웃 & View 규칙 (중요)

-   레이아웃 구조는 `/src/frame/layout` 에서만 정의된다.
-   모든 도메인은 **3단 구조**를 반드시 따른다:

    -   `left / content / right`

-   `views` 는 **UI 표현만 담당**한다.
-   비즈니스 로직을 View 안에 작성하지 않는다.

### 로직이 필요할 경우

1. 기존 composable 사용
2. 없다면 **중단하고 질문**

---

## 5️⃣ 데이터 & 스키마 규칙

-   `/src/system/schemas` 는 **유일한 데이터 표준**이다.
-   도메인 내부에서:

    -   schema 정의 ❌
    -   schema 복사 ❌
    -   임시 모델 생성 ❌

👉 모든 데이터 구조는 **전역 표준을 따른다**.

---

## 6️⃣ Composables 사용 정책

-   `/src/system/composables` 를 우선 사용한다.
-   새 composable 생성 시:

    -   반드시 도메인 내부에 위치
    -   전역 상태에 직접 영향 ❌
    -   system store / eventBus 우회 ❌

---

## 7️⃣ 변경 전 필수 자가 점검

AI는 코드 수정 전에 반드시 아래를 스스로 확인해야 한다.

1. 어떤 파일을 수정하는가?
2. 왜 이 수정이 필요한가?
3. 기존 구조로는 왜 부족한가?
4. 이 변경은 되돌릴 수 있는가?

👉 하나라도 명확하지 않으면 **즉시 중단**한다.

---

## 8️⃣ 금지 행동 목록

-   전체 리팩토링
-   여러 폴더를 넘나드는 정리
-   취향 기반 네이밍 변경
-   구조 재해석
-   조용한 동작 변경 (silent behavior change)

---

## 9️⃣ 안전 기본 원칙 (Fail-Safe)

불확실할 때는 항상:

-   더 적게 수정한다
-   파일 수를 줄인다
-   질문을 선택한다
-   “알아서” 행동하지 않는다

---

## (Quasar / CSS / DOM 구조 통제 규칙)

---

## 🔟 Quasar 프레임워크 관련 절대 규칙

-   Quasar 컴포넌트의 **내부 DOM 구조를 가정하지 않는다**
-   `.q-*` 클래스 구조에 의존한 CSS 작성 금지
-   Quasar 컴포넌트의 스타일 문제는:

    -   ❌ deep selector
    -   ❌ !important
    -   ❌ 과도한 DOM 래핑
        대신 **구조 단순화 또는 전용 래퍼 클래스**를 우선 고려한다

---

## 1️⃣ DOM 구조 최소화 규칙 (중요)

-   UI 구현 시 **불필요한 wrapper div 생성 금지**
-   의미 없는 컨테이너(`<div><div><div>…`) 중첩 금지
-   스타일 목적만을 위한 DOM 추가 금지

### 원칙

> **CSS 문제는 DOM을 늘려서 해결하지 않는다**

AI는 다음을 반드시 지켜야 한다:

-   기존 DOM 구조를 먼저 이해
-   제거 가능한 중첩이 있다면 **추가보다 제거를 우선**

---

## 2️⃣ 인라인 스타일 사용 규칙

-   `style=""` 인라인 스타일은 **원칙적으로 금지**
-   단, 아래 조건을 모두 만족하면 임시 허용:

### 임시 허용 조건

-   계산된 값이 필요한 경우
-   동적 값이며 class로 분리 불가능한 경우
-   **언제든 CSS 파일로 이동 가능한 형태**

📌 허용 시 반드시 주석으로 명시:

```html
<!-- TODO: move to system css when stabilized -->
```

---

## 3️⃣ CSS 중첩 제한 규칙 (강제)

-   SCSS 중첩은 **최대 2단계까지만 허용**
-   3단계 이상 중첩 ❌
-   구조 의존 선택자 (`.a .b .c`) ❌

### 허용 예

```scss
.card {
    .title {
    }
}
```

### 금지 예

```scss
.card {
    .content {
        .inner {
            .text {
            }
        }
    }
}
```

---

## 4️⃣ `:deep()` / `::v-deep` 사용 규칙

-   `deep` selector는 **최후의 수단**
-   사용 전 반드시 아래 질문을 통과해야 한다:

1. DOM 구조를 단순화할 수 없는가?
2. 전용 class를 노출할 수 없는가?
3. 시스템 CSS로 해결할 수 없는가?

👉 하나라도 “예”가 아니면 **deep 사용 금지**

사용 시 반드시 주석으로 이유 명시:

```scss
// deep 사용 이유: Quasar 내부 구조 접근 불가
```

---

## 5️⃣ `!important` 사용 규칙 (거의 금지)

-   `!important`는 **원칙적으로 금지**
-   테마 충돌, Quasar 강제 스타일 덮기 외에는 허용하지 않는다
-   사용 시 반드시 주석으로 근거 명시

```scss
color: red !important; // unavoidable: Quasar inline priority
```

👉 주석 없는 `!important`는 **규칙 위반**

---

## 6️⃣ CSS 위치 전략 (아길이님 구조 반영)

### 기본 전략

1. **컴포넌트 내부 `<style scoped>` 우선**
2. 안정되면 `/src/system/css/nexa-system/` 으로 이동
3. 전역 이동을 항상 염두에 둔 네이밍 사용

### 금지

-   컴포넌트 한정 스타일을 system css에 바로 추가 ❌
-   실험용 스타일을 전역에 추가 ❌

---

## 7️⃣ CSS 파일 구조 준수 규칙

AI는 다음 구조를 **절대 변경하거나 재해석하지 않는다**.

```
레벨 1~3: Quasar + App 기본
레벨 4: 테마
레벨 5: Nexa 시스템 UI 표준
```

-   새로운 시스템 UI 패턴은:

    -   `nexa-system/_*.scss` 로만 추가

-   임의의 전역 scss 파일 생성 ❌

---

## 8️⃣ CSS 작업 시 AI 필수 자가 질문

CSS 수정 전, AI는 반드시 스스로 묻는다:

1. 이 스타일은 DOM 중첩 없이 해결 가능한가?
2. scoped로 충분한가?
3. 나중에 system css로 이동 가능한가?
4. Quasar 내부 구조에 의존하고 있는가?

👉 하나라도 불명확하면 **중단 후 질문**

---
